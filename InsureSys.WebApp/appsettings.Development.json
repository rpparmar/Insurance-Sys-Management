{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // When non-empty, Program.cs maps this onto ConnectionStrings:MasterConnection (Development only). Prefer user-secrets for machine-specific SQL.
  // User secrets: ConnectionStrings:LocalMasterConnection — or env ConnectionStrings__LocalMasterConnection.
  "ConnectionStrings": {
    "LocalMasterConnection": "Server=LAPTOP-21QGI0OU;Database=InsureSysManagement_Master;User Id=sa;Password=SSMS@2019#$$;Encrypt=True;TrustServerCertificate=True;Application Name=InsureSysManagement"
  },
  // Agency restore: Development resolves InsureSys.WebApp/App_Data/{TenantProvisioning:BackupFileName}; SqlServerBackupPath is not used in Development.
  // For hosted non-Dev overrides, use environment variable TenantProvisioning__SqlServerBackupPath (see docker-compose.yml).
  "TenantProvisioning": {}
}
