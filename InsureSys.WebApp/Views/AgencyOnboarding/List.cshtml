@model List<InsuranceSys.Application.DTO.AgencyDetailsDto>
@{
    ViewBag.Title = "All Agencies";
    Layout = "_SuperAdminLayout";
}

<!--begin::Subheader-->
<div class="subheader py-2 py-lg-6 subheader-solid" id="kt_subheader">
    <div class="container-fluid d-flex align-items-center justify-content-between flex-wrap flex-sm-nowrap">
        <div class="d-flex align-items-center flex-wrap mr-1">
            <div class="d-flex align-items-baseline flex-wrap mr-5">
                <h5 class="text-dark font-weight-bold my-1 mr-5">Agency Management</h5>
            </div>
        </div>
        <div class="d-flex align-items-center">
            <a asp-action="AddEditAgency" class="btn btn-primary font-weight-bolder">
                <i class="fas fa-plus mr-1"></i> Onboard New Agency
            </a>
        </div>
    </div>
</div>
<!--end::Subheader-->

<div class="container-fluid">
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["SuccessMessage"]
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @TempData["ErrorMessage"]
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    }

    <div class="card card-custom">
        <div class="card-header">
            <h3 class="card-title">Registered Agencies</h3>
        </div>
        <div class="card-body">
            @if (Model == null || !Model.Any())
            {
                <div class="text-center py-10">
                    <h4 class="text-muted">No agencies/agent registered yet.</h4>
                    <p>Click "Onboard New Agency" to add the first agency.</p>
                </div>
            }
            else
            {
                <div class="table-responsive">
                    <table class="table table-head-custom table-vertical-center table-head-bg" id="tblAgencies">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Agency Code</th>
                                <th>Agency Name</th>
                                <th>Contact Email</th>
                                <th>Database</th>
                                <th>Server</th>
                                <th>Status</th>
                                <th>Created</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @for (int i = 0; i < Model.Count; i++)
                            {
                                var agency = Model[i];
                                <tr>
                                    <td>@(i + 1)</td>
                                    <td><span class="font-weight-bold">@(agency.AgencyCode ?? "—")</span></td>
                                    <td>@agency.AgencyName</td>
                                    <td>@(agency.ContactEmail ?? "-")</td>
                                    <td><code>@agency.DatabaseName</code></td>
                                    <td>@agency.DatabaseServer</td>
                                    <td>
                                        @if (agency.IsActive)
                                        {
                                            <span class="label label-lg label-light-success label-inline font-weight-bold">Active</span>
                                        }
                                        else
                                        {
                                            <span class="label label-lg label-light-danger label-inline font-weight-bold">Inactive</span>
                                        }
                                    </td>
                                    <td>@agency.CreatedAtUtc.ToString("dd MMM yyyy")</td>
                                    <td>
                                        @if (agency.IsActive)
                                        {
                                            <form asp-action="Deactivate" method="post" style="display:inline;"
                                                  onsubmit="return confirm('Are you sure you want to deactivate this agency?');">
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="agencyId" value="@agency.AgencyId" />
                                                <button type="submit" class="btn btn-sm btn-light-danger font-weight-bold">
                                                    Deactivate
                                                </button>
                                            </form>
                                        }
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
        </div>
    </div>
</div>
